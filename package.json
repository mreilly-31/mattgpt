{
  "dependencies": {
    "ts-graphviz": "^3.0.6",
    "tsx": "^4.20.6"
  },
  "name": "mattgpt",
  "version": "1.0.0",
  "description": "ts implementation of andrejs ai course",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:wasm": "cargo build --release --target wasm32-unknown-unknown --manifest-path structures/wasm-rust/Cargo.toml && cp structures/wasm-rust/target/wasm32-unknown-unknown/release/wasm_matmul.wasm structures/wasm/wasm-math.wasm",
    "bench": "npx tsx scripts/bench-runner.ts"
  },
  "author": "matt.reilly@playbookexp.com",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
